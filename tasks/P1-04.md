# ID: P1-04
## Title
i18n cleanup: remove unused deps or implement locale routing + hreflang

## Context
- Current i18n is custom cookie-based:
  - `src/lib/i18n-context.tsx`, `src/lib/i18n/server.ts`, `src/components/i18n/language-switcher.tsx`
- `next-intl` is installed but not used (no matches in `src/`).

## Steps
Option A (minimal): remove unused `next-intl`/`use-intl` dependencies and keep cookie-based i18n.
Option B (preferred for SEO): implement route-based locales (e.g., `/fa/*`, `/en/*`) with `hreflang` and canonical strategy.
1. Pick an approach and document it in `docs/ARCHITECTURE.md`.
2. Ensure RTL/LTR is consistent for SSR and CSR.
3. Add tests that verify `lang`/`dir` correctness on SSR pages.

## Acceptance criteria
- No unused i18n dependencies remain, or locale routing works end-to-end.
- Switching language preserves canonical correctness and does not break sitemap/robots.

## Dependencies
- `P0-06` if adding hreflang/canonical rules for tool pages

## Files to modify (best guess)
- `src/app/layout.tsx`
- `src/lib/i18n-context.tsx`
- `src/lib/i18n/server.ts`
- `package.json`
- `docs/ARCHITECTURE.md`

