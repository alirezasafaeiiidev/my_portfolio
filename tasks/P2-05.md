# ID: P2-05
## Title
Persian date accuracy + numeral formatting unification

## Context
- `src/lib/persian-utils.ts` uses a simplified Gregorian→Jalali conversion and notes it is approximate.
- Toolbox finance/date tools require accurate conversions and consistent Persian digit formatting.

## Steps
1. Decide on a robust local conversion approach (library or improved algorithm) without external services.
2. Replace or wrap `toJalaliDate()` with accurate conversions.
3. Unify numeral formatting across UI (tables, charts, currency).

## Acceptance criteria
- Known conversion test vectors pass (unit tests).
- Finance/date tools display correct dates in fa-IR and correct digits.

## Dependencies
- `P0-03` (if date tools are part of MVP scope)

## Files to modify (best guess)
- `src/lib/persian-utils.ts`
- Add `src/__tests__/lib/persian-utils.test.ts` (extend existing)

