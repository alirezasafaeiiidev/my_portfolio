# ID: P1-07
## Title
Local analytics/telemetry (privacy-preserving, no external services)

## Context
- `src/components/analytics/web-vitals.tsx` exists but is not mounted and does not send anywhere.
- Toolbox needs insight into drop-off without external trackers.

## Steps
1. Implement an opt-in local telemetry store (IndexedDB/localStorage) capturing:
   - tool open/run success/fail
   - performance timings (basic)
2. Add an admin-only dashboard view to read aggregated stats locally (server-rendered, no external).
3. Wire Web Vitals collection behind `NEXT_PUBLIC_ENABLE_WEB_VITALS`.

## Acceptance criteria
- When enabled, basic vitals and tool events are stored locally.
- Admin view can display aggregated counts without leaking PII.

## Dependencies
- `P0-04`

## Files to modify (best guess)
- `src/components/analytics/web-vitals.tsx`
- Add `src/lib/telemetry/*`
- Add `src/app/admin/telemetry/page.tsx` (optional)

