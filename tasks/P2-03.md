# ID: P2-03
## Title
Command palette + keyboard shortcuts (RTL-aware)

## Context
- Power users want fast navigation between tools.

## Steps
1. Add a command palette using existing `cmdk` dependency.
2. Add keyboard shortcuts (e.g., `Ctrl+K`) that work in RTL contexts.
3. Include search results from tool registry and pages.

## Acceptance criteria
- `Ctrl+K` opens palette, searching Persian returns tool routes, Enter navigates.

## Dependencies
- `P1-02`

## Files to modify (best guess)
- Add `src/components/search/command-palette.tsx`
- `src/components/layout/header.tsx`

