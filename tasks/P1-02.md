# ID: P1-02
## Title
Tool search (fast client index, RTL-friendly UX)

## Context
- Search improves conversion (“find the tool in 2 seconds”).
- Current repo has a generic `src/components/search/search-bar.tsx` but it’s not wired to a tool registry.

## Steps
1. Build a small in-memory search index from `src/lib/tools-registry.ts` (title/keywords/description).
2. Add a command palette or search page that supports Persian queries and fuzzy matching (lightweight).
3. Ensure RTL input, Persian digits normalization, and highlighting.

## Acceptance criteria
- Typing “ادغام” or “merge” finds the PDF merge tool.
- Results render instantly and keyboard navigation works.

## Dependencies
- `P0-03`

## Files to modify (best guess)
- `src/components/search/search-bar.tsx`
- Add `src/lib/search/tools-index.ts`
- Add `src/app/(tools)/search/page.tsx` (optional)

