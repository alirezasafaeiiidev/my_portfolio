# ID: P1-10
## Title
Admin UX for toolbox settings/monetization (protected)

## Context
- Admin auth + APIs exist (`src/lib/admin-auth.ts`, `src/app/api/admin/*`).
- Toolbox will need settings toggles (e.g., feature flags, plan config, SW cache control).

## Steps
1. Add an admin section for toolbox settings (flags, plan preview).
2. Add admin controls for:
   - clearing PWA caches (postMessage to SW)
   - toggling “Pro” flags for testing
3. Ensure all pages are `noindex,nofollow`.

## Acceptance criteria
- Admin can toggle feature flags locally and see them reflected in tool UI.
- Admin can trigger cache clear and confirm via UI feedback.

## Dependencies
- `P0-10`
- `P0-02`

## Files to modify (best guess)
- `src/components/admin/admin-dashboard.tsx`
- Add `src/app/admin/toolbox/page.tsx`
- Add `src/app/api/admin/toolbox-settings/route.ts`

