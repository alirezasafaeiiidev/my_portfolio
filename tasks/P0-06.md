# ID: P0-06
## Title
SEO system for tools (metadata, JSON-LD, sitemap coverage, internal linking)

## Context
- Current SEO baseline exists for portfolio pages via:
  - `src/app/layout.tsx` metadata + JSON-LD (`src/lib/seo.ts`)
  - `src/app/sitemap.ts`, `src/app/robots.ts`
- Toolbox needs per-tool SEO, keywords, FAQ blocks, breadcrumbs, and sitemap entries.
- Reference implementation:
  - `_ops/external/asdev-persiantoolbox/lib/tools-registry.ts` (keywords, content, FAQ)
  - `_ops/external/asdev-persiantoolbox/app/sitemap.ts` (indexable tool routes)

## Steps
1. Extend sitemap generation to include tool/category/topic routes generated from `src/lib/tools-registry.ts`.
2. Add per-tool metadata generation (title/description/keywords/canonical) and JSON-LD:
   - `SoftwareApplication` or `WebApplication` per tool
   - FAQPage schema where tool has FAQs
3. Add internal linking blocks:
   - “Related tools” by category
   - “Next recommended step” CTA
4. Ensure admin/API paths remain non-indexed (`src/app/admin/*` already uses `robots: { index: false }`).

## Acceptance criteria
- `/sitemap.xml` contains tool routes and category routes with stable `lastModified`.
- Tool pages render canonical URLs and structured data without CSP violations.
- Lighthouse SEO checks remain passing.

## Dependencies
- `P0-03`
- `P0-01` (CSP nonce) to safely inline JSON-LD

## Files to modify (best guess)
- `src/app/sitemap.ts`
- `src/lib/seo.ts`
- `src/lib/tools-registry.ts` (from `P0-03`)
- Add `src/components/seo/tool-seo-content.tsx` (new)

