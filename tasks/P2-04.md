# ID: P2-04
## Title
PWA update UX polish (prompt + changelog + cache controls)

## Context
- Reference SW update messaging exists in `_ops/external/asdev-persiantoolbox/public/sw.js`.

## Steps
1. Add a persistent update banner/toast when SW reports `UPDATE_AVAILABLE`.
2. Provide user actions:
   - “Update now” (skip waiting + reload)
   - “Later”
3. Add a simple changelog view in-app.

## Acceptance criteria
- Users can see when an update exists and can activate it deterministically.

## Dependencies
- `P0-02`

## Files to modify (best guess)
- `public/sw.js`
- Add `src/components/service-worker/update-banner.tsx`

