# ID: P0-08
## Title
MVP Tool: PDF Merge (local-first, worker-based, Persian UX)

## Context
- Toolbox MVP requires at least one high-demand PDF tool with full UX (upload → process → download).
- Reference implementation exists in `_ops/external/asdev-persiantoolbox/features/pdf-tools/workers/pdf-worker.ts` and routes under `_ops/external/asdev-persiantoolbox/app/(tools)/pdf-tools/merge/merge-pdf/page.tsx`.

## Steps
1. Add a PDF worker and worker client based on the external reference (no server processing).
2. Create route `src/app/(tools)/pdf-tools/merge/merge-pdf/page.tsx` with:
   - drag & drop file input (multiple PDFs)
   - ordering UI (optional for MVP)
   - progress bar + cancel
   - download output with safe filename in Persian/English
3. Enforce hard limits (file count, per-file size, total size).
4. Add FAQ/help copy in fa-IR emphasizing privacy (“پردازش در مرورگر”).
5. Record history entry (from `P0-04`).

## Acceptance criteria
- Merging 2 small PDFs produces a valid downloadable PDF without server calls.
- UI remains responsive during processing (runs in worker).
- Clear error messages for unsupported/encrypted PDFs or size limits.

## Dependencies
- `P0-03`
- `P0-05`
- `P0-04`

## Files to modify (best guess)
- Add `src/features/pdf-tools/workers/pdf-worker.ts`
- Add `src/features/pdf-tools/workerClient.ts`
- Add `src/app/(tools)/pdf-tools/merge/merge-pdf/page.tsx`
- Add `src/components/features/pdf-tools/*` (new)

